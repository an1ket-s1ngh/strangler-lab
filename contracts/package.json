{
  "name": "strangler-lab-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Shared OpenAPI + contract tests for strangler-lab",
  "scripts": {
    "test": "node --test contract.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
